
# 🏆 National Award Roadmap — 待办清单

> 已完成的项目已从清单中移除。以下为仍需推进的工作。

---

### 🔴 P0 — 核心业务缺失项

| 缺陷 | 说明 |
|------|------|
| **无数据持久化** | 无数据库，所有数据都是预生成的静态 JSON，无法操作管理 |
| **功能单一** | 只有"看"（监控大屏），缺少"管"（航线审批/管理/任务调度等业务功能） |
| **无历史数据回溯** | 没有日报/周报/热力分析/趋势图等数据挖掘维度 |
| **智能化程度不足** | A* 虽好但较传统，缺少 AI/ML 预测能力（赛事强调 AI+ 能力） |
| **测试体系空白** | 无单元测试/集成测试，不符合工程化标准 |

> ~~缺乏用户体系~~ → ✅ 已实现 JWT 登录 + 角色分级 + 审计日志
> ~~缺乏数据统计与分析~~ → ✅ 已实现 AnalyticsPanel（ECharts 图表：分时段订单趋势、能耗分布、载重分布）
> ~~移动端适配缺失~~ → ⚠️ 低优先级，3D 大屏天然为桌面端设计

---

### 🟢 已完成的改进项

以下项目已在之前的优化中完成，仅作记录：

- ✅ **用户体系与权限管理**：JWT Token 认证、管理员/调度员/查看者角色分级、操作审计日志
- ✅ **前端路由与多页面架构**：React Router（`/login` → `/dashboard`）、ProtectedRoute 鉴权守卫
- ✅ **数据统计分析面板**：AnalyticsPanel（分时段订单调度趋势、能耗统计、载重分布图表）
- ✅ **性能优化**：getActiveUAVs 零分配、animate 稳定回调、ECharts 懒加载、Web Worker JSON 解析、hoverInfo 减频、二分搜索替代线性扫描
- ✅ **Bug 修复**：城市名动态化、进度条恢复、WeatherOverlay 泄漏修复、依赖检测启动脚本

---

### 🔴 P0 — 仍需实现

#### 1. 数据库持久化
- 引入 PostgreSQL + PostGIS（空间数据库，天然契合地理场景）
- 核心数据表：用户表、航线任务表、无人机注册表、飞行日志表、告警记录表
- 把当前预生成的静态 JSON 迁移到数据库，实现真正的 CRUD
- **加分点**：PostGIS 空间查询能力（如"查询某区域 500m 内的所有航线"）

#### 2. 航线任务管理工作流
- **任务创建**：从前端交互式选择起终点 → 后端生成轨迹 → 提交审批
- **审批流程**：审批中 → 已通过 → 执行中 → 已完成（状态机）
- **任务列表页**：可搜索、可筛选、可排序的任务管理表格
- **加分点**：体现了系统的"业务闭环"能力

---

### 🟡 P1 — 技术深度提升项

#### 3. AI 智能化增强（赛事鼓励 AI+）
- 基于历史轨迹数据，训练一个**航线能耗预测模型**（简单线性回归/随机森林即可）
- 输入：距离、载重、天气、风速 → 输出：预测耗电百分比
- 在创建任务前提供"**AI 预审**"：预测该航线能否安全抵达
- 或者：基于时段/天气/历史数据，预测某个区域的**空域拥堵指数**
- **加分点**：满足赛事 "AI+" 的核心评审导向，使用大赛指定的国产 AI 工具辅助开发

#### 4. 多无人机冲突检测与协同调度
- 同一时段多架无人机的**4D 航路冲突检测**（时间+空间的碰撞预判）
- 可视化展示冲突点（红色闪烁标记）
- 简易调度建议：自动推荐延迟起飞或改变航高
- **加分点**：体现"管控平台"的核心价值

#### 5. 数据分析大屏增强
- 新增独立的 `/analytics` 页面（当前仅为面板，可扩展为独立路由页面）
- 补充内容：
  - 🔥 **起降点热力分布图**（Heatmap）
  - 🚨 **告警统计**（电量告警、禁飞区告警的占比与趋势）
  - 📈 **跨城市对比分析**

---

### 🟠 P2 — 锦上添花

#### 6. 无人机注册管理
- 无人机档案表（型号、最大载重、电池容量、所属运营商）
- 前端提供无人机管理界面
- 为每架无人机分配任务、查看历史飞行记录

#### 7. 测试覆盖
- 后端：`pytest` 覆盖算法核心（A* 寻路、能耗计算）
- 前端：关键 Hook 的单元测试
- **加分点**：展示工程化素养